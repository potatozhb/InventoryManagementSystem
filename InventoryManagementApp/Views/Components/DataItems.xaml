<UserControl x:Class="InventoryManagementApp.Views.Components.DataItems"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:InventoryManagementApp.Views.Components"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <!-- DataGrid -->
            <RowDefinition Height="Auto" />
            <!-- Page Control -->
        </Grid.RowDefinitions>
        <DataGrid Grid.Row="0"
                  ItemsSource="{Binding ItemsView}" 
                  SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                  SelectionMode="Single" 
                  AutoGenerateColumns="False"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*" />
                <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="*" />
                <DataGridTextColumn Header="Quantity" Binding="{Binding Quantity}" Width="Auto" />
            </DataGrid.Columns>
        </DataGrid>

        <!-- Page Control -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button Content="Previous" Command="{Binding PreviousCommand}" Width="80"/>
            <TextBlock Text="{Binding CurrentPage}" Margin="5"/>
            <TextBlock Text="/" Margin="2"/>
            <TextBlock Text="{Binding TotalPages}" Margin="5"/>
            <Button Content="Next" Command="{Binding NextCommand}" Width="80" />
        </StackPanel>
    </Grid>
</UserControl>
